# 警报设置界面流程图

## 修改前后界面对比

```mermaid
flowchart TD
    subgraph A [修改前 - 复杂且不合理]
        A1[警报通知主开关]
        A2[声音提示开关]
        A3[振动提示开关]
        A4[视觉提示开关]
        A5[系统通知开关]
        
        A1 --> A2
        A1 --> A3
        A1 --> A4
        A1 --> A5
        
        A2 -.->|❌ 干扰检测| A6[噪音检测]
        A3 -.->|❌ 干扰检测| A6
        A4 -.->|⚠️ 冗余| A6
        A5 -.->|✅ 合理| A6
    end

    subgraph B [修改后 - 简洁且合理]
        B1[警报通知主开关]
        B2[系统通知开关]
        
        B1 --> B2
        B2 -.->|✅ 合理| B3[噪音检测]
    end

    A --> B
```

## 用户操作流程

```mermaid
flowchart LR
    subgraph C [用户操作流程]
        C1[打开设置页面] --> C2[进入警报设置]
        C2 --> C3{警报主开关}
        
        C3 -->|开启| C4[系统通知开关]
        C3 -->|关闭| C5[所有警报禁用]
        
        C4 -->|开启| C6[通知栏显示警报]
        C4 -->|关闭| C7[仅界面提示]
    end
```

## 技术架构变化

```mermaid
flowchart TB
    subgraph D [修改前技术架构]
        D1[PreferenceKeys模型]
        D2[声音设置字段]
        D3[振动设置字段]
        D4[视觉设置字段]
        D5[系统通知字段]
        
        D1 --> D2
        D1 --> D3
        D1 --> D4
        D1 --> D5
    end

    subgraph E [修改后技术架构]
        E1[PreferenceKeys模型]
        E2[系统通知字段]
        
        E1 --> E2
    end

    D --> E
```

## 核心改进点

### 1. 界面简化
- **从4个选项减少到1个选项**
- **消除用户决策负担**
- **界面更加清晰直观**

### 2. 功能优化
- **自动禁用干扰检测的功能**
- **保留核心有用的功能**
- **提升检测准确性**

### 3. 用户体验
- **设置过程更简单**
- **避免功能冲突**
- **专注核心需求**

## 预期效果

通过这次修改，用户将获得：
- 🎯 **更准确的噪音检测**
- 🎯 **更简洁的设置界面**
- 🎯 **更稳定的设备性能**
- 🎯 **更清晰的用户指引**