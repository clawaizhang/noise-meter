/**
 * 数据库相关常量
 */
export class DatabaseConstants {
  /**
   * 噪音仪表数据库
   */
  static readonly NOISE_METER_DB = 'noise_meter.db';
  static readonly DEVICE_MANAGER_DB = 'device_manager.db';
  static readonly LOCAL_SEND_DB = 'local_send.db';
}